---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import TeamComponent from "../components/TeamComponent.astro";
import { Code } from "astro:components";

const deploymentCode = `
  $ npm install @pendulum-baas/cli # install Pendulum CLI

  $ npx pendulum init     # initialize Pendulum-backed project
  $ npx pendulum deploy   # deploy to AWS
\n
`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/assets/favicon.svg" />
    <title>Pendulum BaaS</title>
  </head>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family:
        -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto", sans-serif;
      line-height: 1.6;
      color: #1a1a1a;
      background: #ffffff;
    }

    .container {
      max-width: 1100px;
      margin: 0 auto;
      padding: 0 2rem;
    }

    .hero {
      padding: 6rem 0 8rem 0;
      text-align: center;
      min-height: 100vh;
      display: flex;
      align-items: center;
      background: linear-gradient(
        180deg,
        #fefefe 0%,
        #f8f6ff 15%,
        #f0ecff 30%,
        #e8e0ff 45%,
        #ddd0ff 65%,
        #d0c0ff 80%,
        #c2b0ff 100%
      );
    }

    .hero h1 {
      font-size: 3.5rem;
      font-weight: 700;
      margin-bottom: 1.5rem;
      letter-spacing: -0.02em;
      color: #666;
    }

    .hero h1 .highlight {
      background: linear-gradient(
        135deg,
        #e67e22 0%,
        #f39c12 30%,
        #f1c40f 70%,
        #f4d03f 100%
      );
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .rotating-text {
      position: relative;
      display: inline-block;
    }

    .rotating-text .word {
      position: absolute;
      top: 0;
      left: 0;
      opacity: 0;
      background: linear-gradient(
        135deg,
        #e67e22 0%,
        #f39c12 30%,
        #f1c40f 70%,
        #f4d03f 100%
      );
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .rotating-text .word.active {
      opacity: 1;
      position: relative;
    }

    .hero .subtitle {
      font-size: 1.25rem;
      color: #666;
      margin-bottom: 3rem;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
      line-height: 1.5;
    }

    .cta-buttons {
      display: flex;
      gap: 1rem;
      justify-content: center;
      flex-wrap: wrap;
    }

    .btn-primary {
      background: linear-gradient(
        135deg,
        #1a1a2e 0%,
        #2d1b69 50%,
        #6a4c93 100%
      );
      color: white;
      padding: 0.875rem 2rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.2s ease;
      font-size: 0.95rem;
      border: none;
      width: 260px;
      text-align: center;
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      opacity: 0.9;
    }

    .btn-secondary {
      background: transparent;
      color: #2d1b69;
      padding: 0.875rem 2rem;
      border-image: linear-gradient(
          135deg,
          #1a1a2e 0%,
          #2d1b69 50%,
          #6a4c93 100%
        )
        1;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.2s ease;
      font-size: 0.95rem;
      width: 260px;
      text-align: center;
      position: relative;
    }

    .btn-secondary::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      border-radius: 6px;
      padding: 2px;
      background: linear-gradient(
        135deg,
        #1a1a2e 0%,
        #2d1b69 50%,
        #6a4c93 100%
      );
      mask:
        linear-gradient(#fff 0 0) content-box,
        linear-gradient(#fff 0 0);
      mask-composite: exclude;
      -webkit-mask:
        linear-gradient(#fff 0 0) content-box,
        linear-gradient(#fff 0 0);
      -webkit-mask-composite: xor;
      z-index: 1;
    }

    .btn-secondary:hover {
      transform: translateY(-1px);
      background: rgba(139, 107, 177, 0.1); /* Soft purple */
    }

    .examples {
      padding: 5rem 0;
    }

    .example {
      display: grid;
      grid-template-columns: 2fr 3fr;
      gap: 4rem;
      align-items: center;
      margin-bottom: 10rem;
      margin-left: 5rem;
      margin-right: 5rem;
    }

    .example:last-child {
      margin-bottom: 0;
    }

    .example.reverse {
      direction: rtl;
    }

    .example.reverse > * {
      direction: ltr;
    }

    .example-content h3 {
      font-size: 2rem;
      font-weight: 600;
      margin-bottom: 1rem;
      line-height: 1.2;
      color: #666;
    }

    .example-content h3 .highlight {
      background: linear-gradient(
        135deg,
        #e67e22 0%,
        #f39c12 30%,
        #f1c40f 70%,
        #f4d03f 100%
      );
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .example-content p {
      font-size: 1.1rem;
      color: #666;
      line-height: 1.6;
    }

    .example-code {
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
    }

    .example-code img,
    video {
      width: 100%;
      height: auto;
      border-radius: 12px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
      display: block;
    }

    /* Override Astro Code component styles for consistency */
    .example-code :global(pre) {
      margin: 0 !important;
      border-radius: 12px !important;
      font-size: 1rem !important;
      line-height: 1.6 !important;
      padding: 0.5rem !important;
    }

    @media (max-width: 968px) {
      .example {
        grid-template-columns: 1fr;
        gap: 2rem;
        text-align: left;
      }

      .example.reverse {
        direction: ltr;
      }

      .example-code {
        order: 2;
      }

      .example-content {
        order: 1;
      }
    }
  </style>
  <body>
    <Header />

    <section class="hero">
      <div class="container">
        <h1>
          Build <span class="highlight">
            <span class="rotating-text">
              <span class="word active">Scalable</span>
              <span class="word">Reactive</span>
              <span class="word">Flexible</span>
            </span>
          </span> Applications
        </h1>
        <p class="subtitle">
          An open-source Backend as a Service that provides developers with
          flexible data schemas, real-time capabilities, and automated cloud
          deployment.
        </p>
        <div class="cta-buttons">
          <a href="/case-study/" class="btn-primary">Read the Case Study</a>
          <a
            href="https://www.github.com/Pendulum-BaaS"
            class="btn-secondary"
            target="_blank">Get Started with Pendulum</a
          >
        </div>
      </div>
    </section>

    <script>
      function initRotatingText() {
        const words = document.querySelectorAll(".rotating-text .word");
        let currentIndex = 0;

        function rotateWords() {
          words[currentIndex].classList.remove("active");
          currentIndex = (currentIndex + 1) % words.length;
          words[currentIndex].classList.add("active");
        }

        setTimeout(() => {
          setInterval(rotateWords, 1000);
        }, 0);
      }

      document.addEventListener("DOMContentLoaded", initRotatingText);
    </script>

    <section class="examples">
      <div class="example">
        <div class="example-content">
          <h3>
            <span class="highlight">Single-Command Deployment</span> to AWS with
            Pendulum CLI Tool
          </h3>
          <p>
            Pendulum is built with AWS architecture and can be easily deployed
            to an existing AWS account using the CLI tool. The self-managed,
            scalable design makes it especially useful for solo devs or small
            teams looking for scalability and rapid prototyping capabilities.
          </p>
        </div>
        <div class="example-code">
          <Code
            code={deploymentCode}
            lang="bash"
            theme="material-theme-palenight"
          />
        </div>
      </div>

      <div class="example reverse">
        <div class="example-content">
          <h3>
            <span class="highlight">AI-Powered</span> Backend Insights
          </h3>
          <p>
            Get insights into your backend state through our MCP server
            integration. Get real-time insights into service health, connected
            clients, and recent data changes all with natural language queries.
          </p>
        </div>
        <div class="example-code">
          <img
            src="/assets/mcp_realtime_monitoring_snippet.png"
            alt="Pendulum MCP Usage"
          />
        </div>
      </div>

      <div class="example">
        <div class="example-content">
          <h3>
            Real-Time <span class="highlight">Data Synchronization</span> Across
            all Clients
          </h3>
          <p>
            Pendulum's built-in reactive capabilities leverage Server-Sent
            Events to ensure your frontend stays in sync with your backend
            state. Custom callback functions give developers the flexibility to
            decide how to handle database change events.
          </p>
        </div>
        <div class="example-code">
          <video controls width="100%" preload="metadata">
            <source
              src="/assets/where-does-pendulum-fit/reactivity.mp4"
              type="video/mp4"
            />
            Your browser does not support the video tag.
          </video>
        </div>
      </div>

      <div class="example reverse">
        <div class="example-content">
          <h3>
            <span class="highlight">Complete Control</span> Over Your Application
            Data
          </h3>
          <p>
            Pendulum's Admin Dashboard gives developers complete visibility and
            control into application data, users, and collection-level
            permissions. Monitor application logs, users, and configure data
            access controls all from a single source of truth.
          </p>
        </div>
        <div class="example-code">
          <img
            src="/assets/home-page-admin-dashboard.gif"
            alt="Admin Dashboard Usage"
          />
        </div>
      </div>
    </section>

    <TeamComponent />

    <Footer />
  </body>
</html>
